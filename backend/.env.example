# Discord OAuth
DISCORD_CLIENT_ID=
DISCORD_CLIENT_SECRET=
DISCORD_REDIRECT_URI=http://localhost:8000/auth/discord/callback

# JWT
JWT_SECRET=change-me-to-a-random-secret
JWT_ALGORITHM=HS256
JWT_EXPIRY_HOURS=72

# Database — use postgresql+asyncpg://user:pass@host/db for Postgres
DATABASE_URL=sqlite+aiosqlite:///./osr_web.db

# Frontend URL (default for CORS and redirects)
FRONTEND_URL=http://localhost:3000

# CORS — comma-separated allowed origins, or leave empty to auto-allow
# localhost + any IP on any port. Set explicitly in production.
# ALLOWED_ORIGINS=http://localhost:3000,http://YOUR_PUBLIC_IP:3000

#SMTP configuration for bug reports via Gmail
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password  # Configure an app password if using Gmail with 2FA: https://support.google.com/accounts/answer/185833
SMTP_FROM=your-email@gmail.com
BUG_REPORT_TO=blazeis292@gmail.com  # Set this to your email to receive bug reports, or leave as is to send to the OSR developer.